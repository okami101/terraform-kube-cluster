initChownData:
  enabled: false

persistence:
  enabled: true
  storageClassName: longhorn
  size: ${persistence_size}

deploymentStrategy:
  type: Recreate

serviceMonitor:
  enabled: true

sidecar:
  dashboards:
    enabled: true
