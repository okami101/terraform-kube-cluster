initChownData:
  enabled: false

persistence:
  enabled: true
  existingClaim: ${pvc_name}

deploymentStrategy:
  type: Recreate

serviceMonitor:
  enabled: true

sidecar:
  dashboards:
    enabled: true
