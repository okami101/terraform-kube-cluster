mysql:
  host: ${mysql.host}
  user: ${mysql.user}
  pass: ${mysql.pass}

serviceMonitor:
  enabled: true

tolerations:
  - key: "node-role.kubernetes.io/storage"
    operator: "Exists"

nodeSelector:
  node-role.kubernetes.io/storage: "true"
