auth:
  existingSecret: ${auth_secret}
  existingSecretPasswordKey: redis-password

master:
  persistence:
    enabled: false

replica:
  persistence:
    enabled: false
  replicaCount: 1

metrics:
  enabled: true
  serviceMonitor:
    enabled: true
