auth:
  existingSecret: ${auth_secret}
  existingSecretPasswordKey: redis-password

replica:
  persistence:
    enabled: false
  replicaCount: 3

sentinel:
  enabled: true

metrics:
  enabled: true
  serviceMonitor:
    enabled: true
