metrics:
  enabled: true
  serviceMonitor:
    enabled: true

influxdb:
  tolerations:
    - key: node-role.kubernetes.io/storage
      effect: NoSchedule
  nodeSelector:
    node-role.kubernetes.io/primary: "true"
