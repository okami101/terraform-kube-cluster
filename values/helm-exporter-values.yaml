config:
  helmRegistries:
    registryNames:
      - prometheus-community
      - traefik
      - jaegertracing
      - grafana
      - gitea
      - concourse
      - sstarcher
      - kured
      - longhorn
      - bitnami-labs
tolerations:
  - key: "node-role.kubernetes.io/monitor"
    operator: "Exists"
