initChownData:
  enabled: false

persistence:
  enabled: true
  storageClassName: longhorn

deploymentStrategy:
  type: Recreate

serviceMonitor:
  enabled: true

sidecar:
  dashboards:
    enabled: true
