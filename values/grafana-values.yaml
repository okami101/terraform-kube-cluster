initChownData:
  enabled: false

persistence:
  enabled: true
  storageClass: nfs-client

serviceMonitor:
  enabled: true

sidecar:
  dashboards:
    enabled: true

tolerations:
  - key: node-role.kubernetes.io/monitor
    operator: Exists
