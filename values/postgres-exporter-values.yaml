config:
  datasourceSecret:
    name: postgres-secret
    key: datasources

serviceMonitor:
  enabled: true

tolerations:
  - key: "node-role.kubernetes.io/data"
    operator: "Exists"

nodeSelector:
  node-role.kubernetes.io/data: "true"
